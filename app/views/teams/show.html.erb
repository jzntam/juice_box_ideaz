<%= @team.title %>
<br>
<%#= @team.user.first_name %>
<% @team.membered_users.each do |element| %>
  <%= element.first_name %>
  <%= element.last_name %>
  <br>
<% end %>

<% @team.shared_ideas.each do |element| %>
  <%= element.title %>
  <%= element.description %>
  <br>
<% end %>
  


<br><br>

<%= link_to "Edit", edit_team_path(@team) %> | 

<%= link_to "Delete", team_path(@team), method: :delete, data: {confirm: "Are you sure?"} %>
